// settings.js

/*
import {
    applyTheme,
    getCurrentTheme
} from "../theme.js";

const select =
    document.getElementById("theme-select");

if (select) {

    select.value =
        getCurrentTheme();

    select.addEventListener(
        "change",
        () => {
            applyTheme(select.value);
        }
    );
}
*/